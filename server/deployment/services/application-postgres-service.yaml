apiVersion: v1
kind: Service
metadata:
  name: application-postgres
  namespace: resiaiac
spec:
  clusterIP: None
  selector:
    app: application-postgres
  ports:
    - port: 5432
      targetPort: 5432
