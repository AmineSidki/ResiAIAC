apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: resiaiac
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      name: http
      targetPort: 80
      nodePort: 30080

    - port: 8333
      name: s3
      targetPort: 8333
      nodePort: 30081
